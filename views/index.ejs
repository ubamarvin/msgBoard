<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>Mini Messageboard</h1>

  <ul>
    <% messages.forEach(message => { %>
      <li>
        <strong><%= message.user %></strong> at <%= message.added.toLocaleString() %>: <%= message.text %>
        <a href="#">(open)</a>
      </li>
    <% }) %>
  </ul>

  <div class="container">
    <a href="/new">Add a new message</a>
  </div>
</body>
</html>
